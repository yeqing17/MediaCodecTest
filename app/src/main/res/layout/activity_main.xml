<?xml version="1.0" encoding="utf-8"?>
<!-- Landscape split layout, dark diagnostic theme: controls + video on the left,
     live stats card on the right, everything visible on one screen. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true"
    android:orientation="horizontal"
    android:paddingLeft="12dp"
    android:paddingRight="10dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- ===== Left column: controls + video ===== -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:orientation="vertical">

        <!-- Preset dropdown inside an input-style shell -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@drawable/bg_input">

            <Spinner
                android:id="@+id/presetSpinner"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="8dp" />
        </FrameLayout>

        <!-- URL + action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/urlInput"
                style="@style/MCT.EditText"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:hint="udp://@239.x.x.x:port | http://... | 文件选择" />

            <Button
                android:id="@+id/openFileBtn"
                style="@style/MCT.Button.Ghost"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="6dp"
                android:minWidth="52dp"
                android:text="文件" />

            <Button
                android:id="@+id/muteBtn"
                style="@style/MCT.Button.Ghost"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="6dp"
                android:minWidth="52dp"
                android:text="静音" />

            <Button
                android:id="@+id/stopBtn"
                style="@style/MCT.Button.Danger"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="6dp"
                android:minWidth="56dp"
                android:text="Stop" />

            <Button
                android:id="@+id/playBtn"
                style="@style/MCT.Button.Primary"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="6dp"
                android:minWidth="64dp"
                android:text="Play" />
        </LinearLayout>

        <!-- Toggles -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="horizontal">

            <CheckBox
                android:id="@+id/forceSoftware"
                style="@style/MCT.CheckBox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="软解 Soft Decode" />

            <CheckBox
                android:id="@+id/autoReconnectBox"
                style="@style/MCT.CheckBox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="18dp"
                android:checked="true"
                android:text="自动重连 Auto Reconnect" />
        </LinearLayout>

        <!-- Player: left main body, thin frame around the surface -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            android:layout_weight="1"
            android:background="@drawable/bg_video_border"
            android:padding="2dp">

            <androidx.media3.ui.PlayerView
                android:id="@+id/playerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#FF000000"
                app:resize_mode="fit"
                app:surface_type="surface_view"
                app:use_controller="false" />
        </FrameLayout>

        <!-- Collapsible playurl config -->
        <TextView
            android:id="@+id/advancedToggle"
            style="@style/SectionLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="PLAYURL CONFIG  [+]" />

        <LinearLayout
            android:id="@+id/configContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:orientation="vertical"
            android:visibility="gone" />

        <Button
            android:id="@+id/getUrlBtn"
            style="@style/MCT.Button.AccentGhost"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="6dp"
            android:visibility="gone"
            android:text="GET URL → 拼播放地址" />

        <!-- Export row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal">

            <Button
                android:id="@+id/exportLogBtn"
                style="@style/MCT.Button.Ghost"
                android:layout_width="0dp"
                android:layout_height="38dp"
                android:layout_weight="1"
                android:text="Export Log" />

            <Button
                android:id="@+id/exportReportBtn"
                style="@style/MCT.Button.Ghost"
                android:layout_width="0dp"
                android:layout_height="38dp"
                android:layout_marginStart="6dp"
                android:layout_weight="1"
                android:text="Export Report" />
        </LinearLayout>
    </LinearLayout>

    <!-- ===== Right column: live stats ===== -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginStart="10dp"
        android:layout_weight="2"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:letterSpacing="0.12"
                android:text="LIVE STATS · 实时统计"
                android:textColor="@color/text_dim"
                android:textSize="13sp"
                android:textStyle="bold" />

            <View
                android:id="@+id/statsDot"
                android:layout_width="9dp"
                android:layout_height="9dp"
                android:background="@drawable/bg_dot" />
        </LinearLayout>

        <ScrollView
            android:id="@+id/statsScroll"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="6dp"
            android:layout_weight="1"
            android:background="@drawable/bg_panel"
            android:overScrollMode="never">

            <TextView
                android:id="@+id/statsView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:lineSpacingExtra="2dp"
                android:padding="12dp"
                android:textColor="@color/text"
                android:textIsSelectable="true"
                android:textSize="14sp" />
        </ScrollView>
    </LinearLayout>

</LinearLayout>
